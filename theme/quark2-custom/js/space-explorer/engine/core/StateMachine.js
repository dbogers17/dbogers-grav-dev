export class StateMachine{constructor(initial,transitions={}){this.value=initial;this.transitions=transitions;this.listeners=new Set()}can(next){const allowed=this.transitions[this.value];return !allowed||allowed.includes(next)}set(next){if(next===this.value)return true;if(!this.can(next))return false;const previous=this.value;this.value=next;for(const fn of this.listeners)fn({previous,next});return true}subscribe(fn){this.listeners.add(fn);return()=>this.listeners.delete(fn)}}
