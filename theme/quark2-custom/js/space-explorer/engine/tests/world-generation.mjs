import assert from'node:assert/strict';import{AzureReach}from'../world/AzureReach.js';assert.ok(AzureReach.asteroids.length>=120);assert.ok(AzureReach.traffic.length>=5);assert.ok(AzureReach.locations.filter(x=>x.type==='outpost').length>=2);const station=AzureReach.locations.find(x=>x.id==='helix');assert.ok(station.radius>=150&&station.dockRadius>=300);console.log('World generation tests passed');
