import assert from'node:assert/strict';import{Camera}from'../core/Camera.js';import{EXPLORER_OPTIONS}from'../data/ExplorerOptions.js';const c=new Camera();c.adjustZoom(.4);assert.equal(c.targetZoom,1.4);c.adjustZoom(9);assert.equal(c.targetZoom,1.6);c.resetZoom();assert.equal(c.targetZoom,1);assert.ok(EXPLORER_OPTIONS.helmetStyle.includes('None'));console.log('Integration recovery tests passed');
