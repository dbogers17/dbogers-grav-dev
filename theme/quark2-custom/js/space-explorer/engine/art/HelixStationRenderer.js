import{FACTION_STYLES}from'../data/FactionStyles.js';
export class HelixStationRenderer{constructor(assetManager){this.assets=assetManager;this.style=FACTION_STYLES.helix;this.asset=this.assets.load('helix-hub',this.style.station.asset)}draw(ctx,x,y,time=0){const size=this.style.station.scale,pulse=.5+.5*Math.sin(time*.0022);ctx.save();ctx.translate(x,y);ctx.fillStyle='rgba(0,0,0,.58)';ctx.beginPath();ctx.ellipse(22,28,size*.46,size*.35,0,0,Math.PI*2);ctx.fill();if(this.asset.ready){ctx.save();ctx.globalCompositeOperation='source-over';ctx.imageSmoothingEnabled=true;ctx.drawImage(this.asset.image,-size/2,-size/2,size,size);ctx.restore()}else{ctx.fillStyle='#14181e';ctx.strokeStyle='#e59a2f';ctx.lineWidth=4;ctx.beginPath();ctx.arc(0,0,size*.22,0,Math.PI*2);ctx.fill();ctx.stroke()}ctx.strokeStyle=`rgba(82,184,230,${.2+.15*pulse})`;ctx.lineWidth=2;ctx.setLineDash([8,14]);ctx.beginPath();ctx.arc(0,0,size*.55,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);for(let i=0;i<24;i++){const a=i/24*Math.PI*2,r=size*(i%3===0?.31:.38),lx=Math.cos(a)*r,ly=Math.sin(a)*r;ctx.fillStyle=i%4===0?'#e59a2f':'#52b8e6';ctx.shadowBlur=8;ctx.shadowColor=ctx.fillStyle;ctx.beginPath();ctx.arc(lx,ly,2+(i%3===0),0,Math.PI*2);ctx.fill()}ctx.shadowBlur=0;ctx.restore()}}
