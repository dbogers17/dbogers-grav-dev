function hash(x,y,s=0){let n=Math.imul((x|0)^0x9e3779b9,(y|0)^0x85ebca6b)+(s|0);n=Math.imul(n^(n>>>16),0x7feb352d);return((n^(n>>>15))>>>0)/4294967295}
export class CalmInfiniteSpaceRenderer{constructor(){this.tile=960}draw(ctx,camera,w,h,time=0){ctx.fillStyle='#010103';ctx.fillRect(0,0,w,h);const layers=[{p:.025,count:30,a:.45},{p:.065,count:18,a:.62},{p:.13,count:7,a:.75}];for(const l of layers){const ox=camera.x*l.p,oy=camera.y*l.p,tx0=Math.floor((ox-w/2)/this.tile)-1,ty0=Math.floor((oy-h/2)/this.tile)-1;for(let tx=tx0;tx<=tx0+Math.ceil(w/this.tile)+2;tx++)for(let ty=ty0;ty<=ty0+Math.ceil(h/this.tile)+2;ty++){const bx=tx*this.tile-ox+w/2,by=ty*this.tile-oy+h/2;for(let i=0;i<l.count;i++){const x=bx+hash(tx,ty,i)*this.tile,y=by+hash(ty,tx,i+71)*this.tile,r=.4+hash(tx+17,ty-23,i)*1.45,k=hash(tx,ty,i+29);ctx.globalAlpha=l.a*(.55+hash(tx,ty,i+47)*.45);ctx.fillStyle=k>.96?'#ffd59a':k>.91?'#9fdcff':'#e2e5e8';ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}}}ctx.globalAlpha=1;for(let i=0;i<7;i++){const p=.04+(i%3)*.035,x=((hash(i,4)*w*2-camera.x*p)%(w+260)+w+260)%(w+260)-130,y=((hash(i,9)*h*2-camera.y*p*1.07)%(h+260)+h+260)%(h+260)-130,r=7+hash(i,33)*18;ctx.fillStyle='rgba(78,71,63,.25)';ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}const clouds=[['rgba(92,42,30,.055)',.017,.24,.68,440],['rgba(55,38,78,.05)',.023,.74,.3,510]];for(const[color,p,fx,fy,r]of clouds){const x=((fx*w-camera.x*p)%(w+r*2)+w+r*2)%(w+r*2)-r,y=((fy*h-camera.y*p*.83)%(h+r*2)+h+r*2)%(h+r*2)-r,g=ctx.createRadialGradient(x,y,0,x,y,r);g.addColorStop(0,color);g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.fillRect(x-r,y-r,2*r,2*r)}const sx=w*.86,sy=h*.15,g=ctx.createRadialGradient(sx,sy,0,sx,sy,70);g.addColorStop(0,'rgba(255,244,210,.88)');g.addColorStop(.12,'rgba(239,153,70,.55)');g.addColorStop(.5,'rgba(140,49,25,.06)');g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.fillRect(sx-70,sy-70,140,140)} }
