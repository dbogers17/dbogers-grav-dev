import{FACTION_STYLES}from'../data/FactionStyles.js';
export class StableHelixRenderer{constructor(assetManager){this.asset=assetManager.load('helix-hub',FACTION_STYLES.helix.station.asset);this.size=520;this.cache=null;this.asset.promise.then(()=>this.build())}build(){const c=document.createElement('canvas');c.width=this.size;c.height=this.size;const x=c.getContext('2d');x.imageSmoothingEnabled=true;x.drawImage(this.asset.image,0,0,this.size,this.size);this.cache=c}draw(ctx,x,y,time){x=Math.round(x);y=Math.round(y);ctx.save();ctx.translate(x,y);ctx.fillStyle='rgba(0,0,0,.62)';ctx.beginPath();ctx.ellipse(18,24,238,172,0,0,Math.PI*2);ctx.fill();if(this.cache){ctx.imageSmoothingEnabled=false;ctx.drawImage(this.cache,-260,-260)}for(let i=0;i<16;i++){const a=i/16*Math.PI*2,r=i%2?205:174;ctx.fillStyle=i%4?'#62c9ee':'#e49a32';ctx.shadowBlur=7;ctx.shadowColor=ctx.fillStyle;ctx.beginPath();ctx.arc(Math.cos(a)*r,Math.sin(a)*r,2.5,0,Math.PI*2);ctx.fill()}ctx.shadowBlur=0;ctx.restore()}}
