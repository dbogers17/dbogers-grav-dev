export class StationRenderer{
 draw(ctx,point,station,time=0){const r=station.radius||145,pulse=.55+.45*Math.sin(time*.002);ctx.save();ctx.translate(point.x,point.y);
  ctx.fillStyle='rgba(0,0,0,.5)';ctx.beginPath();ctx.ellipse(18,22,r*1.18,r*.72,-.1,0,Math.PI*2);ctx.fill();
  ctx.strokeStyle=`rgba(34,211,238,${.13+.1*pulse})`;ctx.lineWidth=2;ctx.setLineDash([8,12]);ctx.beginPath();ctx.arc(0,0,station.dockRadius||270,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  const core=ctx.createRadialGradient(-22,-28,8,0,0,r*.72);core.addColorStop(0,'#dff8ff');core.addColorStop(.18,'#3d91c1');core.addColorStop(.6,'#153754');core.addColorStop(1,'#050b14');ctx.fillStyle=core;ctx.strokeStyle='#75c8ec';ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,r*.55,0,Math.PI*2);ctx.fill();ctx.stroke();
  const arms=[[-1,0,1.25],[1,0,1.05],[0,-1,.9],[0,1,.78],[-.72,-.72,.62],[.72,.72,.58]];for(let i=0;i<arms.length;i++){const[x,y,len]=arms[i],angle=Math.atan2(y,x),length=r*len,width=24+(i%2)*6;ctx.save();ctx.rotate(angle);const g=ctx.createLinearGradient(0,-width/2,length,width/2);g.addColorStop(0,'#335a78');g.addColorStop(.4,'#172b40');g.addColorStop(1,'#08111d');ctx.fillStyle=g;ctx.strokeStyle='#6da5c3';ctx.fillRect(r*.38,-width/2,length,width);ctx.strokeRect(r*.38,-width/2,length,width);for(let l=r*.55;l<r*.38+length-10;l+=22){ctx.fillStyle=i%2?'#fbbf24':'#67e8f9';ctx.shadowBlur=8;ctx.shadowColor=ctx.fillStyle;ctx.fillRect(l,-width/2+4,5,3)}ctx.shadowBlur=0;ctx.restore()}
  ctx.fillStyle='#0b1927';ctx.strokeStyle='#8ac9e5';ctx.lineWidth=2;ctx.beginPath();ctx.roundRect(r*1.25,-35,72,70,10);ctx.fill();ctx.stroke();ctx.fillStyle='#67e8f9';ctx.fillRect(r*1.37,-22,30,4);ctx.fillStyle='#fbbf24';ctx.fillRect(r*1.37,18,30,4);
  for(let i=0;i<5;i++){const a=i/5*Math.PI*2+.4,x=Math.cos(a)*r*.74,y=Math.sin(a)*r*.74;ctx.fillStyle=i%2?'#fbbf24':'#67e8f9';ctx.shadowBlur=9;ctx.shadowColor=ctx.fillStyle;ctx.beginPath();ctx.arc(x,y,3,0,Math.PI*2);ctx.fill()}ctx.shadowBlur=0;ctx.fillStyle='#e6f7ff';ctx.font='700 14px sans-serif';ctx.textAlign='center';ctx.fillText(station.name||'Helix Station',0,r*.72);ctx.restore()}
}
