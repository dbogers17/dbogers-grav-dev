function hash(x,y,s=0){let n=(x*374761393+y*668265263+s*69069)|0;n=(n^(n>>>13))*1274126177;return((n^(n>>>16))>>>0)/4294967295}
export class InfiniteSpaceRenderer{constructor(){this.tile=720}draw(ctx,camera,w,h,time=0){const t=this.tile,layers=[{p:.03,count:80},{p:.08,count:48},{p:.16,count:18}];ctx.fillStyle='#010103';ctx.fillRect(0,0,w,h);for(const layer of layers){const ox=camera.x*layer.p,oy=camera.y*layer.p,minX=Math.floor((ox-w)/t)-1,maxX=Math.ceil((ox+w)/t)+1,minY=Math.floor((oy-h)/t)-1,maxY=Math.ceil((oy+h)/t)+1;for(let tx=minX;tx<=maxX;tx++)for(let ty=minY;ty<=maxY;ty++){const bx=tx*t-ox+w/2,by=ty*t-oy+h/2,seed=tx*997+ty*619;for(let i=0;i<layer.count;i++){const x=bx+hash(tx,ty,i)*t,y=by+hash(ty,tx,i+31)*t,r=.35+hash(seed,i,7)*1.8,kind=hash(seed,i,11);ctx.globalAlpha=.28+hash(seed,i,13)*.72;ctx.fillStyle=kind>.96?'#ffd9a1':kind>.91?'#9fdcff':'#eef1f5';ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}}}ctx.globalAlpha=1;const sx=w*(.82+Math.sin(time*.00003)*.015),sy=h*.18,g=ctx.createRadialGradient(sx,sy,0,sx,sy,95);g.addColorStop(0,'rgba(255,245,210,.96)');g.addColorStop(.08,'rgba(255,184,92,.82)');g.addColorStop(.32,'rgba(204,75,38,.15)');g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.fillRect(sx-95,sy-95,190,190);const clouds=[['rgba(90,35,45,.12)',.025,.33,.62,460],['rgba(78,54,32,.10)',.04,.68,.76,380],['rgba(52,33,81,.09)',.018,.18,.28,520]];for(const[color,p,fx,fy,r]of clouds){const x=((fx*w-camera.x*p)%(w+r*2)+w+r*2)%(w+r*2)-r,y=((fy*h-camera.y*p)%(h+r*2)+h+r*2)%(h+r*2)-r,n=ctx.createRadialGradient(x,y,0,x,y,r);n.addColorStop(0,color);n.addColorStop(1,'transparent');ctx.fillStyle=n;ctx.fillRect(x-r,y-r,r*2,r*2)}for(let i=0;i<20;i++){const x=((hash(i,19)*w*2-camera.x*.11)%(w+200)+w+200)%(w+200)-100,y=((hash(i,47)*h*2-camera.y*.11)%(h+200)+h+200)%(h+200)-100,r=4+hash(i,81)*16;ctx.fillStyle='rgba(91,82,71,.32)';ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}}
}
