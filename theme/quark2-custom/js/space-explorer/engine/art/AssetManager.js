export class AssetManager{constructor(){this.images=new Map()}load(id,url){if(this.images.has(id))return this.images.get(id);const image=new Image(),promise=new Promise((resolve,reject)=>{image.onload=()=>resolve(image);image.onerror=()=>reject(new Error(`Asset failed: ${url}`))});image.src=url;const asset={image,promise,ready:false};promise.then(()=>asset.ready=true);this.images.set(id,asset);return asset}get(id){return this.images.get(id)}}
