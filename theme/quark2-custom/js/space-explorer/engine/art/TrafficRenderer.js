export class TrafficRenderer{draw(ctx,traffic,camera,w,h,time){for(const ship of traffic){const phase=(time*.00003*ship.speed+ship.phase)%1,angle=ship.angle+phase*Math.PI*2,x=ship.cx+Math.cos(angle)*ship.radius,y=ship.cy+Math.sin(angle)*ship.radius*.55,p=camera.worldToScreen(x,y,w,h,1);ctx.save();ctx.translate(p.x,p.y);ctx.rotate(angle+Math.PI/2);ctx.fillStyle='rgba(0,0,0,.4)';ctx.fillRect(-7,7,16,7);const g=ctx.createLinearGradient(-7,-10,7,8);g.addColorStop(0,'#dff8ff');g.addColorStop(.3,ship.color);g.addColorStop(1,'#07111d');ctx.fillStyle=g;ctx.strokeStyle='#b8dff0';ctx.beginPath();ctx.moveTo(0,-12);ctx.lineTo(7,8);ctx.lineTo(0,5);ctx.lineTo(-7,8);ctx.closePath();ctx.fill();ctx.stroke();ctx.fillStyle=ship.role==='patrol'?'#ef4444':'#67e8f9';ctx.shadowBlur=7;ctx.shadowColor=ctx.fillStyle;ctx.fillRect(-2,8,4,6);ctx.shadowBlur=0;ctx.restore()}}
}
