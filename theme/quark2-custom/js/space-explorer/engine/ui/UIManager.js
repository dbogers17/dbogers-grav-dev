import{STATES,VERSION}from'../core/Config.js';
const fmt=s=>s?new Date(s).toLocaleString():'Never';
export class UIManager{constructor({machine,bus,saveSystem,getShip}){this.machine=machine;this.bus=bus;this.saveSystem=saveSystem;this.getShip=getShip;this.root=document.querySelector('#sl-space-ui-root');this.returnState=STATES.FLIGHT;this.root.addEventListener('click',e=>this.handle(e));this.root.addEventListener('submit',e=>this.submit(e));this.machine.subscribe(({previous,next})=>{if(next===STATES.SETTINGS)this.returnState=previous;this.render()});this.render()}button(l,a,k=''){return`<button type="button" data-action="${a}" class="${k}">${l}</button>`}handle(e){const a=e.target.closest('[data-action]')?.dataset.action,id=e.target.closest('[data-slot]')?.dataset.slot;if(id&&a==='load')return this.bus.emit('load',id);if(id&&a==='delete')return this.bus.emit('delete',id);({slots:()=>this.machine.set(STATES.SLOTS),create:()=>this.machine.set(STATES.CREATE),menu:()=>this.machine.set(STATES.MENU),map:()=>this.machine.set(STATES.MAP),settings:()=>this.machine.set(STATES.SETTINGS),close:()=>this.machine.set(this.machine.value===STATES.SETTINGS?this.returnState:STATES.FLIGHT),exit:()=>this.bus.emit('exit'),save:()=>this.bus.emit('save')}[a]||(()=>{}))()}submit(e){if(e.target.id!=='sl-create-save')return;e.preventDefault();const name=new FormData(e.target).get('name');this.bus.emit('create',name)}render(){const s=this.machine.value,ship=this.getShip?.();this.root.className=`sl-engine-ui state-${s}`;if(s===STATES.FLIGHT){const speed=Math.round(Math.hypot(ship?.vx||0,ship?.vy||0));this.root.innerHTML=`<header class="sl-engine-hud"><button data-action="map">Map</button><div><small>GALAXY</small><strong>Azure Reach</strong></div><div><small>SPEED</small><strong id="sl-speed">${speed} u/s</strong></div><div><small>VERSION</small><strong>v${VERSION}</strong></div><button data-action="menu">Menu</button></header><aside class="sl-vitals"><label>SHIELD <span><i id="sl-shield" style="width:${ship.shield/ship.maxShield*100}%"></i></span><b>${Math.ceil(ship.shield)}</b></label><label>HULL <span><i id="sl-hull" style="width:${ship.hull/ship.maxHull*100}%"></i></span><b>${Math.ceil(ship.hull)}</b></label><label>BOOST <span><i id="sl-boost" style="width:${ship.boost/ship.maxBoost*100}%"></i></span><b>${Math.ceil(ship.boost)}</b></label></aside>`;return}if(s===STATES.START){this.root.innerHTML=`<section class="sl-screen sl-start"><small>EARLY ACCESS · v${VERSION}</small><h1>SPACE EXPLORER</h1><p>Choose one of three save slots.</p>${this.button('Save games','slots')}${this.button('Settings','settings')}</section>`;return}if(s===STATES.SLOTS){const slots=this.saveSystem.list();this.root.innerHTML=`<section class="sl-screen sl-slots"><header><div><small>LOCAL PROFILES</small><h2>Save games</h2></div>${this.button('Back','back-start')}</header><div class="sl-slot-list">${slots.map(x=>`<article><div><b>${x.explorerName}</b><small>${x.galaxy} · ${x.ship}</small><small>Last saved: ${fmt(x.lastSavedAt)}</small><code>${x.recoveryCode}</code></div><div>${this.button('Continue','load')} ${this.button('Delete','delete','danger')}</div></article>`.replaceAll('data-action="load"',`data-action="load" data-slot="${x.slotId}"`).replaceAll('data-action="delete"',`data-action="delete" data-slot="${x.slotId}"`)).join('')}</div>${slots.length<3?this.button('Create new explorer','create'):''}<p class="sl-note">Explorer names must be unique. Server-wide validation activates with the save API.</p></section>`;this.root.querySelector('[data-action="back-start"]')?.addEventListener('click',()=>this.machine.set(STATES.START));return}if(s===STATES.CREATE){this.root.innerHTML=`<section class="sl-screen"><header><div><small>NEW PROFILE</small><h2>Create explorer</h2></div>${this.button('Back','slots')}</header><form id="sl-create-save"><label>Explorer name<input name="name" minlength="3" maxlength="18" pattern="[A-Za-z0-9 _-]+" required autocomplete="off"></label><button type="submit">Create save</button></form><p id="sl-form-error"></p></section>`;return}if(s===STATES.MENU){const active=this.bus.activeSave?.();this.root.innerHTML=`<section class="sl-panel"><header><div><small>FLIGHT COMPUTER</small><h2>Game menu</h2></div>${this.button('Close','close')}</header><p class="sl-last-save">Last saved: <b>${fmt(active?.lastSavedAt)}</b><br>Recovery code: <code>${active?.recoveryCode||'Unavailable'}</code></p><div class="sl-grid">${this.button('Resume','close')}${this.button('Save game','save')}${this.button('Galaxy map','map')}${this.button('Settings','settings')}${this.button('Exit','exit','danger')}</div></section>`;return}if(s===STATES.MAP){this.root.innerHTML=`<section class="sl-map-screen"><header><div><small>NAVIGATION</small><h2>Azure Reach</h2></div>${this.button('Close map','close')}</header><div class="sl-map-canvas"><span style="left:50%;top:50%">Helix Station</span><span style="left:68%;top:35%">Broken Beacon</span><span style="left:27%;top:69%">Cobalt Field</span><span style="left:80%;top:23%">North Gate</span><b style="left:50%;top:56%">YOU</b></div></section>`;return}if(s===STATES.SETTINGS){this.root.innerHTML=`<section class="sl-panel"><header><div><small>CONFIGURATION</small><h2>Settings</h2></div>${this.button('Close','close')}</header><div class="sl-settings"><label>Quality<select><option>Low</option><option selected>High</option><option>Ultra</option></select></label><label>FPS limit<select><option>30</option><option selected>60</option><option>120</option></select></label><label>Master volume<input type="range" min="0" max="100" value="80"></label><label><input type="checkbox"> Reduced motion</label></div></section>`}}
}
