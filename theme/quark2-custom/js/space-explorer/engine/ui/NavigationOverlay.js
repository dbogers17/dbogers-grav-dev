export class NavigationOverlay{draw(ctx,nav,ship,camera,w,h){const n=nav.info(ship);if(!n)return;const p=camera.worldToScreen(n.x,n.y,w,h,1),inside=p.x>70&&p.x<w-70&&p.y>105&&p.y<h-70,angle=Math.atan2(p.y-h/2,p.x-w/2);ctx.save();ctx.strokeStyle='rgba(229,154,47,.65)';ctx.setLineDash([9,8]);ctx.beginPath();ctx.moveTo(w/2,h/2);ctx.lineTo(inside?p.x:w/2+Math.cos(angle)*Math.min(w,h)*.38,inside?p.y:h/2+Math.sin(angle)*Math.min(w,h)*.38);ctx.stroke();ctx.setLineDash([]);const x=inside?p.x:w/2+Math.cos(angle)*Math.min(w,h)*.42,y=inside?p.y:h/2+Math.sin(angle)*Math.min(w,h)*.42;ctx.translate(x,y);ctx.rotate(angle+Math.PI/2);ctx.fillStyle='#f59e0b';ctx.beginPath();ctx.moveTo(0,-14);ctx.lineTo(9,9);ctx.lineTo(-9,9);ctx.closePath();ctx.fill();ctx.restore();ctx.fillStyle='rgba(5,7,10,.88)';ctx.fillRect(w-250,92,230,64);ctx.fillStyle='#f3f4f6';ctx.font='700 13px sans-serif';ctx.fillText(n.name,w-236,114);ctx.fillStyle='#e59a2f';ctx.font='12px sans-serif';ctx.fillText(`${Math.round(n.distance)} u · ${Math.round(n.bearing)}° · ETA ${Math.ceil(n.eta)}s`,w-236,138)}}
