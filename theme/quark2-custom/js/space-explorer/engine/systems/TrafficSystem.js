export class TrafficSystem{constructor(definitions=[]){this.ships=definitions.map(d=>({...d,index:0,t:0,x:d.route[0][0],y:d.route[0][1],vx:0,vy:0}))}update(dt){for(const s of this.ships){const target=s.route[(s.index+1)%s.route.length],dx=target[0]-s.x,dy=target[1]-s.y,d=Math.hypot(dx,dy);if(d<12){s.index=(s.index+1)%s.route.length;continue}const speed=Math.min(s.speed,d*1.4),tx=dx/d*speed,ty=dy/d*speed,k=1-Math.exp(-2.4*dt);s.vx+=(tx-s.vx)*k;s.vy+=(ty-s.vy)*k;s.x+=s.vx*dt;s.y+=s.vy*dt}}}
