import{loadSave,saveGame}from'./save-manager.js';const c=document.querySelector('#sl-space-canvas');if(c){const x=c.getContext('2d'),state=loadSave(),k={},p={x:innerWidth/2,y:innerHeight/2},s={x:state.x||innerWidth/2,y:state.y||innerHeight/2,vx:0,vy:0,a:0,shield:state.shield,hull:state.hull};const stars=Array.from({length:220},(_,i)=>({x:i*181%innerWidth,y:i*73%innerHeight,z:.2+(i%10)/11}));function size(){let d=Math.min(devicePixelRatio||1,2);c.width=innerWidth*d;c.height=innerHeight*d;x.setTransform(d,0,0,d,0,0)}size();addEventListener('resize',size);addEventListener('pointermove',e=>{p.x=e.clientX;p.y=e.clientY});addEventListener('keydown',e=>{if(/INPUT|TEXTAREA|SELECT/.test(document.activeElement?.tagName||''))return;k[e.key.toLowerCase()]=1;if(['w','a','s','d',' '].includes(e.key.toLowerCase()))e.preventDefault()});addEventListener('keyup',e=>k[e.key.toLowerCase()]=0);const panel=document.querySelector('#sl-space-panel');function close(){panel.hidden=true}function button(){panel.querySelector('#sl-close-panel')?.addEventListener('click',close)}document.querySelector('#sl-space-menu').onclick=()=>{panel.hidden=false;panel.innerHTML='<h2>Game menu</h2><p>This is the integration-safe foundation for ships, quests, settings and multiplayer.</p><button id="sl-save-now">Save game</button> <button id="sl-close-panel">Close</button>';panel.querySelector('#sl-save-now').onclick=()=>saveGame({...state,x:s.x,y:s.y,shield:s.shield,hull:s.hull});button()};document.querySelector('#sl-space-map').onclick=()=>{panel.hidden=false;panel.innerHTML='<h2>Galaxy map</h2><p><strong>Azure Reach</strong> is unlocked.</p><p>Crystal Void, Red Forge, Ancient Horizon and Event Horizon unlock through quests.</p><button id="sl-close-panel">Close</button>';button()};function frame(){x.fillStyle='#020611';x.fillRect(0,0,innerWidth,innerHeight);for(const q of stars){x.fillStyle=`rgba(140,200,255,${q.z*.5})`;x.fillRect(q.x,q.y,q.z*1.6,q.z*1.6)}let dx=(k.d?1:0)-(k.a?1:0),dy=(k.s?1:0)-(k.w?1:0),m=Math.hypot(dx,dy)||1;s.vx+=dx/m*.18;s.vy+=dy/m*.18;s.vx*=.95;s.vy*=.95;s.x=(s.x+s.vx+innerWidth)%innerWidth;s.y=(s.y+s.vy+innerHeight)%innerHeight;s.a=Math.atan2(p.y-s.y,p.x-s.x);x.save();x.translate(s.x,s.y);x.rotate(s.a+Math.PI/2);x.fillStyle='#d9f1ff';x.shadowBlur=18;x.shadowColor='#5ce0ff';x.beginPath();x.moveTo(0,-21);x.lineTo(12,14);x.lineTo(0,8);x.lineTo(-12,14);x.closePath();x.fill();x.restore();document.querySelector('#sl-space-coordinates').textContent=`${Math.round(s.x)}, ${Math.round(s.y)}`;document.querySelector('#sl-space-health').textContent=`${s.shield} / ${s.hull}`;requestAnimationFrame(frame)}requestAnimationFrame(frame)}
