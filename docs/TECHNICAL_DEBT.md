# Technical debt

- Remove legacy full-game files after v0.9.0 passes live verification.
- Move world definitions to validated JSON data.
- Add fixed, deterministic seeded world generation.
- Add browser-run integration tests with Playwright when a Node test runner is available.
- Add a server protocol specification before any multiplayer endpoint is exposed.
