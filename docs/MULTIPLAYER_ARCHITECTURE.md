# Multiplayer foundation

Planned architecture is authoritative: clients send inputs, while the server validates movement, damage, inventory, mission rewards and world interactions. Planned services: lobby, friends-only rooms, sector simulation, reconnect, shared missions and PostgreSQL persistence. Multiplayer remains disabled until authentication and server validation are implemented.
